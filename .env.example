# Required for local development. Vercel deployments can authenticate with OIDC.
AI_GATEWAY_API_KEY=your_vercel_ai_gateway_key

# Optional. The model is intentionally not exposed in the product UI.
AI_GATEWAY_MODEL=openai/gpt-5-mini
